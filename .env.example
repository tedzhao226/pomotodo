# Copy to .env and adjust. Never commit .env.
POSTGRES_USER=pomotodo
POSTGRES_PASSWORD=change-me
POSTGRES_DB=pomotodo

# App connection string (compose derives this for the app service too).
POMOTODO_DATABASE_URL=postgresql+psycopg://pomotodo:change-me@db:5432/pomotodo
